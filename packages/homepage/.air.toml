# Air config for live reload
root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "npx @tailwindcss/cli -i ./styles.css -o ./static/styles.css --minify && go build -o ./tmp/main ."
  delay = 1000
  exclude_dir = ["tmp", "node_modules", "static"]
  exclude_file = []
  exclude_regex = ["_test\\.go"]
  exclude_unchanged = true
  follow_symlink = false
  include_ext = ["go", "html", "toml", "json", "css"]
  include_dir = []
  kill_delay = "0s"
  log = "build-errors.log"
  send_interrupt = false
  stop_on_error = false

[color]
  app = ""
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  time = false

[misc]
  clean_on_exit = true
